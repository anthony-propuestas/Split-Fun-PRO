# Build output
dist/
node_modules/
.wrangler/

# ── Secrets & environment variables ──────────────────────────────────────────
# Never commit these — they contain API keys, client IDs, and secrets
.env
.env.local
.env.*.local
.dev.vars

# ── Cloudflare generated files ────────────────────────────────────────────────
worker-configuration.d.ts

# ── MCP config (contiene API keys) ───────────────────────────────────────────
.mcp.json
.vscode/mcp.json

# ── TestSprite (contiene API keys y credenciales de proxy) ────────────────────
testsprite_tests/tmp/

# ── Seeds de desarrollo local (no para producción) ────────────────────────────
seed-test-user.sql

# ── OS / IDE ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/settings.json
*.log
